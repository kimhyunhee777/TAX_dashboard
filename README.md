# TAX_dashboard — 세법 개정·판례·뉴스 알림

세법 개정, 관련 판례, 관련 뉴스를 놓치지 않고 클라이언트에게 알아듣기 쉬운 형태로 안내하기
위한 알림 피드입니다. 매일 자동으로 (1) `provisions.json`에 등록된 조문의 원문이 바뀌었는지,
(2) `precedents.json`에 등록된 주제와 관련된 새 판례가 나왔는지, (3) `news_topics.json`에
등록된 검색어로 새 뉴스 기사가 나왔는지 확인해서 알림을 생성합니다. LLM을 쓰지 않기 때문에
API 키/과금 없이 동작합니다 (뉴스는 Google 뉴스 RSS를 사용하며 이것도 키가 필요 없습니다).

## 구조

- `provisions.json` — 추적할 법령/조문 목록 (직접 편집해서 늘려가세요)
- `precedents.json` — 추적할 판례 검색어/주제 목록 (직접 편집)
- `news_topics.json` — 추적할 뉴스 검색어/주제 목록 (직접 편집)
- `clients.json` — 업종별 관심 태그 (제조업/소비재·유통업/IT·서비스업 등, 직접 편집)
- `data/state.json` / `data/alerts.json` — 법령 개정 감지 상태/알림 (스크립트가 자동 갱신)
- `data/precedent_state.json` / `data/precedent_alerts.json` — 판례 감지 상태/알림 (스크립트가 자동 갱신)
- `data/news_state.json` / `data/news_alerts.json` — 뉴스 감지 상태/알림 (스크립트가 자동 갱신)
- `scripts/check_amendments.py` — 법령 개정 감지 스크립트 (diff 기반, 변경 전/후 비교)
- `scripts/backfill_recent_amendments.py` — (1회성) law.go.kr 연혁법령(eflaw) API로 각 법령의 "현재 시행본"과 "그 바로 이전 공식본"을 찾아 같은 조문을 실제로 비교한다. 추적을 막 시작해서 diff로는 안 잡히는, 추적 이전에 이미 일어난 개정도 진짜 변경 전/후 원문으로 채워 넣는다 (더미 텍스트가 아니라 실제 이전 공식본 원문). 이후로는 `check_amendments.py`가 계속 이어서 실제 diff 알림을 만든다.
- `scripts/check_precedents.py` — 새 판례 감지 스크립트 (사건명을 그대로 "쉬운 설명"으로 사용). 처음 추적을 시작하는 주제는 최근 1년 이내 판례까지는 바로 알림으로 채우고, 그보다 오래된 이력은 베이스라인으로만 저장한다.
- `scripts/check_news.py` — 새 뉴스 감지 스크립트 (헤드라인만 저장, 본문은 저장하지 않음, 노이즈성 결과 제외)
- `scripts/send_digest.py` — 오늘 새로 감지된 알림을 이메일로 요약 발송 (SMTP 설정 시에만 동작)
- `scripts/common.py` — 세 감지 스크립트가 공유하는 JSON 입출력/오래된 알림 정리 유틸리티
- `api/lawtext.py` — law.go.kr 원문 조회 프록시 (Vercel 서버리스, "원문 보기" 링크용)
- `.github/workflows/check-amendments.yml` — 매일 07:00 KST 자동 실행 (개정/판례/뉴스 확인 → 이메일 다이제스트)

화면은 법령 개정 / 판례 / 뉴스 세 섹션으로 나뉘어 표시되고, 각 섹션은 알림이 있을 때만 보입니다.
각 섹션은 기본적으로 접혀 있고(제목과 건수만 보임), 헤더를 클릭하면 펼쳐져서 카드 목록이 보입니다.
법령 개정 diff는 줄 단위로 다르면 그 줄 전체를 색칠하는 게 아니라, 같은 자리의 이전/이후 문단을
단어 단위로 다시 비교해서 실제로 달라진 단어만 굵게 강조합니다 (조문 한 문단이 원래 길기 때문에,
한 단어만 바뀌어도 문단 전체가 빨강/초록으로 칠해지면 뭐가 바뀐 건지 알아보기 어려워서 이렇게
처리했습니다).

## 준비 과정

1. **법제처 오픈API OC 발급**: https://www.law.go.kr 오픈API 신청에서 OC(신청ID)를 발급받습니다.
2. **GitHub Secret 등록**: 저장소 Settings → Secrets and variables → Actions에 `LAW_API_OC` 등록 (자동 개정 감지 워크플로가 사용).
3. **Vercel 배포**: 이 저장소를 Vercel에 연결하고, 프로젝트 환경변수에도 `LAW_API_OC`를 동일하게 등록 (`api/lawtext.py`의 "원문 보기" 프록시가 사용).
4. **provisions.json / precedents.json / news_topics.json / clients.json 채우기**: 지금은
   실무에서 자주 쓰이는 세액공제·세액감면 조문(조세특례제한법·법인세법) 14건 + 손금불산입·매입세액불공제·
   부당행위계산부인 관련 조문(대손금·세금과공과금·자본거래·자산평가손실·감가상각비·기부금·
   기업업무추진비·과다경비·업무무관비용·업무용승용차·지급이자·대손충당금·부당행위계산부인 —
   법인세법, 매입세액불공제 — 부가가치세법) 15건, 관련 판례 검색어 24건, 관련 뉴스 검색어 21건이
   들어있습니다. 손금불산입 계열 항목은 업종을 가리지 않고 대부분의 법인에 해당하는 내용이라
   `"tags": ["전체"]`로 넣어뒀습니다.
   `clients.json`은 개별 회사가 아니라 업종 6종
   (제조업/소비재·유통업/IT·서비스업/건설업/수출·해외법인/개인사업자·자영업)으로 구성되어
   있어서, 클라이언트가 어떤 업종인지에 따라 관련 조문·판례·뉴스만 걸러볼 수 있습니다. 필요에
   따라 자유롭게 추가/삭제하세요.
   - 조문에 가지번호가 있는 경우(예: 제29조의8) `"jo": "29", "branch": "8"`처럼 `branch` 필드를 추가하세요. 가지번호가 없는 본조(예: 제24조)는 `branch` 필드를 생략하면 됩니다.
   - `precedents.json`/`news_topics.json`의 `query`는 각각 law.go.kr 판례 검색·Google 뉴스 검색에 그대로 들어가는 검색어입니다. 너무 넓은 단어(예: "세액공제" 단독)는 결과가 너무 많아 알림이 잦아지니, 구체적인 제도명으로 좁혀서 넣는 걸 권장합니다.
   - 특정 업종 하나에 묶기 애매한 일반 뉴스(예: 매년 발표되는 세법개정안)는 `"tags": ["전체"]`로 넣으면 업종 필터와 무관하게 모든 업종에서 보입니다.
   - `provisions.json`에 새 조문을 추가한 뒤 `python scripts/check_amendments.py`로 베이스라인을 한 번 잡고 나서, `python scripts/backfill_recent_amendments.py`를 실행하면 바로 이전 공식본과 비교한 실제 변경 전/후 알림도 채워집니다 (1회만 실행하면 됨, 매일 자동 실행 워크플로에는 포함되어 있지 않습니다).
5. **(선택) 이메일 다이제스트 설정**: 저장소 Settings → Secrets and variables → Actions에 아래 값을 등록하면, 매일 자동 확인이 끝난 뒤 오늘 새로 감지된 알림을 요약한 이메일이 발송됩니다. 하나라도 비어있으면 자동으로 발송을 건너뜁니다.
   - `SMTP_HOST`, `SMTP_PORT`(보통 587), `SMTP_USER`, `SMTP_PASS` — 메일 발송용 SMTP 계정 (Gmail이면 계정 자체 비밀번호가 아니라 "앱 비밀번호"를 발급받아 사용해야 합니다)
   - `DIGEST_TO` — 받는 사람 이메일 주소 (여러 명이면 쉼표로 구분)
   - `DIGEST_FROM` — 보내는 사람 주소 (생략하면 `SMTP_USER`와 동일하게 사용)

## 로컬에서 확인하기

```bash
pip install -r scripts/requirements.txt
LAW_API_OC=발급받은값 python scripts/check_amendments.py
LAW_API_OC=발급받은값 python scripts/check_precedents.py
python scripts/check_news.py   # 뉴스는 OC 불필요
```

처음 실행하면 각각의 `data/*_state.json`에 베이스라인만 저장되고 알림은 생기지 않습니다.
이후 실행에서 조문 원문이 바뀌거나, 새 판례·뉴스가 나오면 각 `data/*_alerts.json`에 알림이
추가됩니다. `index.html`을 정적 서버로 열어 알림 피드, 업종 필터, 벨 배지를 확인할 수 있습니다.

- 업종을 선택한 상태에서 **"🔗 공유 링크 복사"**를 누르면 그 업종만 필터링된 링크(`?industry=xxx`)가 클립보드에 복사됩니다 — 클라이언트에게 바로 보낼 수 있습니다.

GitHub Actions 워크플로는 Actions 탭에서 "Run workflow"로 수동 실행해 바로 확인할 수도 있습니다.
데이터가 무한정 쌓이지 않도록 뉴스는 60일, 법령 개정/판례는 365일이 지난 알림을 매 실행마다
자동으로 정리합니다 (`scripts/common.py`의 `prune_old`).

## 알려진 한계

- law.go.kr "현행법령" 서비스는 시행된 조문만 보여줍니다. 공포는 됐지만 아직 시행 전인 개정은
  시행일이 될 때까지 감지되지 않을 수 있습니다.
- 조문 끝에 붙는 `<개정 2020.1.1, ...>` 이력 꼬리표는 법 조문 본문이 아니라 참고용 기록이고,
  law.go.kr이 만드는 통합본마다 이 꼬리표가 항상 일관되게 누적되지는 않아서(타법개정 등으로
  병렬 계보가 생기는 경우), 개정 감지·백필·화면의 diff 비교 모두 이 태그는 제외하고 비교합니다
  — 실제 조문 내용이 같으면 태그만 다르더라도 "개정됨"으로 표시하지 않습니다.
  `check_amendments.py`는 추가로, law.go.kr이 요청마다 미세하게 다른 포맷으로 응답을 줄 때
  생기는 가짜 감지(해시는 다른데 내용은 동일한 경우)도 알림 없이 조용히 걸러냅니다.
- 백필(`backfill_recent_amendments.py`)은 "현재 시행본 vs 그 바로 이전 공식본" 단 한 번의
  비교만 합니다. 그 이전 공식본에서도 이미 반영되어 있던 더 오래된 개정은 잡히지 않습니다
  (그 경우엔 여러 단계 이전 공식본까지 비교하도록 스크립트를 확장해야 합니다).
- 화면에는 "쉬운 설명" 문구를 따로 보여주지 않습니다 (개정 조문은 변경 전/후 diff 자체가
  이미 가장 정확한 설명이고, LLM 없이 만든 템플릿 문장은 오히려 군더더기라 판단해 뺐습니다).
  다만 이메일 다이제스트 본문에는 여전히 diff 기반 요약 문구가 들어갑니다 — 품질을 높이고
  싶다면 `scripts/check_amendments.py`의 `build_plain_summary()` 함수를 LLM 호출로 교체하면
  됩니다. 판례는 사건명 자체가 쟁점을 설명하는 문장이라 화면에 그대로 보여줍니다.
- 로그인 기능은 없습니다. 이메일 다이제스트를 설정하지 않으면 사이트 방문 시 배지로만 확인됩니다.
- 이메일 다이제스트는 지금은 **모든 알림을 하나로 묶어 한 곳(또는 몇몇 수신자)에** 보내는 방식입니다. 클라이언트별로 관련 있는 알림만 골라 각자에게 따로 보내는 것까지는 지원하지 않습니다 — 필요하면 `scripts/send_digest.py`를 클라이언트별로 반복 호출하도록 확장하면 됩니다.
- **징벌적 목적의 손해배상금 손금불산입(법인세법 제21조의2)은 조문만 추적하고 판례/뉴스 검색어는 넣지 않았습니다.** 확인 시점 기준으로 관련 검색 결과가 0건이었기 때문입니다 — 실제 사례가 나오기 시작하면 `precedents.json`/`news_topics.json`에 검색어를 추가하면 됩니다.
- **판례 본문(판시사항/판결요지)은 제공하지 않습니다.** 세무 판례는 대부분 국세법령정보시스템에서
  수집된 자료라 law.go.kr의 판례 상세 조회 API로는 본문을 가져올 수 없고, 검색 결과에 포함된
  사건명·사건번호·선고일자 등 메타데이터만 표시합니다. 다만 이 도메인의 사건명 자체가 쟁점을
  요약하는 문장으로 되어 있어("OOO 세액공제 대상 여부" 등) 실질적으로 쉬운 설명 역할을 합니다.
  전문이 필요하면 알림 카드의 "law.go.kr에서 판례 보기" 링크로 이동해서 확인해야 합니다.
- **뉴스도 헤드라인만 저장합니다.** 기사 본문은 언론사 저작물이라 그대로 가져와 저장하지
  않고, 제목·출처·발행일과 원문 링크만 보관합니다("기사 원문 보기"를 눌러야 전문을 볼 수
  있음). Google 뉴스 RSS를 사용하므로 링크가 news.google.com 리다이렉트 주소로 뜨는 점도
  참고하세요.
